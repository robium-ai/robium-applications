# 2026-07-10 — nav-trial

- [architect] fired correctly on app kickoff via explicit Skill invocation; requirement checklist mapped 1:1 onto what brainstorming had gathered (robot type, task, sim/real, GPU, local/remote); Platform gotchas section directly covered our exact case (macOS/Apple Silicon → Docker, headless → foxglove, Jazzy-not-Lyrical for Nav2) — no gaps at routing time ✓
- [architect] Jazzy-over-Lyrical exception for the Nav2 vertical verified live by the robium-architect agent: navigation2#6123 still open, no Lyrical rows on the ROS package index (2026-07-10) — skill's stated exception and issue reference exactly current ✓
- [gazebo] figured out from scratch (agent research, not skill content): Gazebo's lidar is a render-based `gpu_lidar` sensor, so even `gz sim -s` headless needs a working render engine (EGL/llvmpipe) in a GPU-less container — architect's references don't mention headless render-sensor requirements; candidate content for the gazebo skill's headless/server section
- [nav2] figured out from scratch (agent research): TB3's Jazzy Gazebo integration expects `TwistStamped` on /cmd_vel while Nav2's stamped-output default (`enable_stamped_cmd_vel`) must be explicitly aligned — a known integration trap not surfaced by any skill; candidate for nav2 (or ros2) gotchas. Confirmed during planning: asked both nav2 and ros2 skills explicitly ("TwistStamped vs Twist on Jazzy" in the invocation args) — neither SKILL.md nor nav2's common-failures.md §6 (cmd_vel diagnostics, the natural home) mentions it
- [testing]→[ros2] broken routing: testing skill says "launch_testing/pytest mechanics → ros2" (twice, incl. the pyramid table), but the ros2 skill has zero launch_testing content — no reference file, no SKILL.md section (`grep -rn launch_testing skills/ros2/` → empty). Anyone following the route hits a dead end; fix is either a launch-testing section/reference in ros2 or rewording testing's route
- [integration] compose-patterns.md's macOS gotcha is load-bearing and accurate for this build: DDS multicast doesn't cross Docker bridge networks and network_mode:host isn't real on Docker Desktop/macOS — this drove nav-trial's design to one-container-per-scenario (all nodes of a profile in a single `ros2 launch`), avoiding cross-container DDS entirely ✓ (design-time; runtime verification pending)
- [integration] compose profiles gotcha hit for real: with every service behind a `profiles:` key (the compose-patterns one-image/one-service-per-profile shape), bare `docker compose build` exits 0 with `level=warning msg="No services to build"` — it silently builds NOTHING, so a `make build` wrapping it "succeeds" without producing an image. Fix verified: explicitly naming a service (`docker compose build sim`) auto-activates its profile and builds; `--profile "*"` also works. Candidate gotcha for integration's compose-patterns (any Makefile/CI `build` target over an all-profiles compose file needs an explicit service or profile)
- [environments]/[gazebo] R4 (arm64 apt coverage) resolved cleanly ✓: the brief's Dockerfile apt set (ros-jazzy-navigation2, nav2-bringup, slam-toolbox, turtlebot3-gazebo, turtlebot3-navigation2, foxglove-bridge, ros-gz, python3-colcon-common-extensions) all installed from stock packages.ros.org + ports.ubuntu.com on linux/arm64 (ros:jazzy-ros-base-noble base) — no 404s, no OSRF-repo contingency needed; full image build 191s on Apple Silicon, `ros2 pkg list` in-container shows nav_trial_bringup + all four stack packages
- [gazebo] figured out from scratch (Step 1 inspection, Task 3): upstream `turtlebot3_gazebo/launch/turtlebot3_world.launch.py` (2.3.x, Jazzy apt) is NOT headless-usable as an include — it hardcodes a gz GUI client include (`gz_args: '-g -v2'`, unconditional `ld.add_action(gzclient_cmd)`) and hardcodes server args `gz_args: ['-r -s -v2 ', world]` with no DeclareLaunchArgument to inject `--headless-rendering` → Path B required. The sub-launch files ARE cleanly reusable though: `spawn_turtlebot3.launch.py` (takes x_pose/y_pose, reads TURTLEBOT3_MODEL env, starts BOTH ros_gz_sim `create` and the `parameter_bridge` with `params/turtlebot3_<model>_bridge.yaml`; image_bridge skipped for burger) and `robot_state_publisher.launch.py` (takes use_sim_time, reads urdf from turtlebot3_gazebo/urdf/). Best composition: own gz include via `ros_gz_sim/launch/gz_sim.launch.py` with headless gz_args + upstream spawn + rsp includes. Candidate content for gazebo skill's headless section.
- [nav2] R3 evidence (Task 3, before even running): `turtlebot3_gazebo/params/turtlebot3_burger_bridge.yaml` bridges `cmd_vel` as ros_type `geometry_msgs/msg/TwistStamped` (ROS_TO_GZ), with an inline upstream comment "If you use Twist, you need to change the type to Twist" — so on Jazzy TB3, plain Twist pubs to /cmd_vel are silently useless and Nav2 needs `enable_stamped_cmd_vel: true`. Runtime confirmation pending in Step 7.
- [gazebo] R1 KILLED, first rung ✓: `gz sim -s -r --headless-rendering -v2 turtlebot3_world.world` (via ros_gz_sim gz_sim.launch.py, `ros:jazzy-ros-base-noble` arm64 image, no GPU, no X, no extra mesa/EGL apt packages beyond what ros-jazzy-ros-gz pulls in) rendered the TB3 burger's gpu_lidar fine — check_scan.py printed `PASS: /scan publishing` on the FIRST try; zero OGRE2/EGL errors in gz console output (only benign warnings: fuel LocalCache dir missing, sdf gz_frame_id element copy). No fallback rungs needed.
- [integration]/[ros2] gotcha (Task 3 Step 5): `docker compose exec <svc> python3 ...` bypasses the image ENTRYPOINT, so the ROS env is NOT sourced → `ModuleNotFoundError: No module named 'rclpy'`. Fix: `docker compose exec sim /entrypoint.sh python3 /ws/tests/check_scan.py` (or `bash -c 'source ... && ...'`). `compose run` uses the entrypoint; `compose exec` does not — worth a line in integration's compose-patterns.
- [gazebo] stale command in Task 3 brief Step 6: `gz stats` does NOT exist in modern gz (gz-tools2/Harmonic — it was a Gazebo Classic CLI). Modern equivalent: `gz topic -e -t /world/<name>/stats -n 5` (here `/world/default/stats`; a root `/stats` alias also exists) and read `real_time_factor`. Also: on ROS Jazzy the `gz` CLI is VENDORED (`ros-jazzy-gz-tools-vendor` → under /opt/ros/jazzy), so `gz` is not on PATH until the ROS env is sourced — plain `docker compose exec sim bash -lc 'gz ...'` gives `command not found`.
- [gazebo] Task 3 Step 6 RTF measurement: headless software-rendered TB3 burger in turtlebot3_world on Apple Silicon Docker (arm64, no GPU): RTF ≈ 0.99 (cumulative sim 93.398s / real 94.333s; instantaneous samples 0.987–1.052, step 1 ms). Sim runs at essentially real time — Task 7 SMOKE_TIMEOUT can assume RTF 1.0 with margin.
- [nav2] R3 CONFIRMED at runtime (Task 3 Step 7, the evidence the skills lack): on Jazzy TB3 burger + ros_gz, `ros2 topic info -v /cmd_vel` shows the only subscriber is `ros_gz_bridge` with type `geometry_msgs/msg/TwistStamped` (RELIABLE/VOLATILE). The brief's plain-Twist drive command is a silent no-op: `ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist ...` hangs on "Waiting for at least 1 matching subscription(s)" (type mismatch → never matches) and odom stays at spawn. `geometry_msgs/msg/TwistStamped "{twist: {linear: {x: 0.1}}}"` drives immediately: odom x 0.0 → 0.385 m in ~3 s. Task 6 must set Nav2 `enable_stamped_cmd_vel: true` (and any teleop/driver must publish TwistStamped).
- [foxglove] worked as documented ✓ (Task 4 user checkpoint): browser app.foxglove.dev → Open connection → ws://localhost:8765 against foxglove_bridge running inside the headless arm64 container; user confirmed live topic list + /scan + TF with zero bridge-side config beyond the SKILL.md launch line. One non-skill friction: Docker Desktop had stopped between sessions → "socket closed" in Foxglove; symptom looks like a bridge bug but is the daemon — worth a one-line note in foxglove's gotchas (bridge unreachable ≠ bridge broken; check the container runtime first)

# 2026-07-11 — nav-trial (continued)

- [nav2] params base decision (Task 6 Step 1, executed during Task 5): `/opt/ros/jazzy/share/turtlebot3_navigation2/param/burger.yaml` (jazzy apt, 433 lines) won as the base — it has 13 of the 14 sections nav2_bringup jazzy launches (amcl, bt_navigator, docking_server, controller_server, both costmaps, map_server, map_saver, planner_server, behavior_server, waypoint_follower, collision_monitor, velocity_smoother) with TB3-tuned values (DWB acc limits per turtlebot3_simulations#75, robot_radius 0.1). Only `smoother_server` was missing → grafted verbatim from the robium nav2 skill example (`nav2-params-diffdrive.yaml`, mirrors nav2_bringup jazzy). Frames: TB3 file itself uses base_footprint for amcl/collision_monitor and base_link for bt_navigator/costmaps/behavior_server — kept as-is per brief. Note: the TB3 file has ZERO `use_sim_time` keys (relies on nav2_bringup's RewrittenYaml rewrite); set explicitly true in all 14 sections.
- [nav2] R3 alignment came for free: `enable_stamped_cmd_vel: true` exists under exactly that name and is ALREADY set in all 5 cmd_vel-publishing sections of the shipped TB3 jazzy param files (controller_server, behavior_server, velocity_smoother, collision_monitor, docking_server — grep across burger/waffle/waffle_pi/burger_cam shows 5 hits each). The TB3 maintainers already aligned params with their TwistStamped bridge; the trap only bites people starting from nav2_bringup's own nav2_params.yaml (which the robium skill example mirrors — that example does NOT set enable_stamped_cmd_vel and drops collision_monitor). Candidate skill note: "on TB3 jazzy start from turtlebot3_navigation2 params, not nav2_bringup's".
- [ros2] gotcha not in any skill (hit via the Task 5 brief's run_slam.sh snippet): `set -u` BEFORE `source /opt/ros/jazzy/setup.bash` kills the script — "`/opt/ros/jazzy/setup.bash: line 8: AMENT_TRACE_SETUP_FILES: unbound variable`" (exit 1, container dies instantly). ROS setup scripts are not `set -u` clean; order must be source-then-`set -u`. Candidate one-liner for ros2 skill's workspace/scripting section — every wrapper script around `ros2 launch/run` in strict bash mode hits this.
- [nav2] SLAM-then-navigate pattern as commonly stated ("bringup with slam:=True skips map_server/AMCL; launch slam_toolbox alongside") is WRONG on jazzy and cost a full failed run (0/9 waypoints): jazzy `nav2_bringup/launch/slam_launch.py` (included by bringup_launch.py when slam:=True) launches its OWN slam_toolbox — `online_sync_launch.py` — plus a map_saver_server and a `lifecycle_manager_slam`. Adding online_async alongside → BOTH sync+async slam nodes run, lifecycle managers fight (`Unable to start transition 1 from current state inactive/3 from active: Transition is not registered`, `Failed to make transition 'TRANSITION_ACTIVATE' for LifecycleNode '/slam_toolbox'`), bond heartbeats die (`CRITICAL FAILURE: SERVER controller_server IS DOWN after not receiving a heartbeat for 4000 ms`), planner stops acknowledging (`Timed out while waiting for action server to acknowledge goal request for compute_path_to_pose`), every goal FAILED instantly. Won't-move checklist rung 1 (lifecycle) caught it. Fix that works: `navigation_launch.py` (nav servers only) + own `online_async_launch.py` include; alternative is bringup slam:=True with NO extra slam include (slam_toolbox params then go inside nav2_params.yaml — slam_launch.py's HasNodeParams check picks them up). Candidate correction for the nav2 skill's SLAM-then-navigate section.
- [nav2] figured out from scratch (run 3 root-cause, won't-move checklist rung 1 lifecycle): on Docker Desktop/macOS (12 vCPU / 8 GB VM) the whole container stalls ~8 s of wall clock at Nav2 activation + first goal (gz physics+lidar render, ceres scan matching, DWB 20x40 samples, foxglove param storm all spiking at once — NO process logged 193→200.8 s). Nav2's default `bond_timeout` 4.0 s then fires `CRITICAL FAILURE: SERVER controller_server IS DOWN after not receiving a heartbeat for 4000 ms. Shutting down related nodes.` and the whole stack self-destructs → every subsequent goal instantly rejected. Trap: jazzy `navigation_launch.py` hard-codes lifecycle_manager params to `{autostart, node_names}` — `bond_timeout` CANNOT be set via params_file/RewrittenYaml. Only escape: launch the servers yourself with your own lifecycle_manager (`bond_timeout: 0.0` disables bond checking, the documented Nav2 knob). Candidate for nav2 skill: containers/constrained hosts section — bond_timeout vs Docker stalls, and the navigation_launch.py params limitation.
- [nav2] figured out from scratch (run 3, checklist rung 6 cmd_vel): shipped TB3 jazzy `burger.yaml` collision_monitor scan source has `source_timeout: 0.2` while the burger lidar publishes at 5 Hz (0.2 s period) — the newest scan is ALWAYS at the staleness edge, so under any jitter collision_monitor logs `Latest source and current collision monitor node timestamps differ on 0.2xx seconds. Ignoring the source.` then `Robot to stop due to invalid source` and zeroes cmd_vel: robot cannot move even with a healthy stack. Fix: scan source_timeout 1.0. This is an upstream TB3 params bug worth a nav2-skill gotcha (collision_monitor source_timeout must exceed the sensor period).
- [nav2] jazzy navigation_launch.py launches `route_server` (nav2_route) and `docking_server` (opennav_docking) unconditionally — neither has a section in TB3's burger.yaml (docking does, route doesn't); docking_server spams `Failed to get parameters: parameter 'dock_database' is not initialized` warns every ~15 s. Both activated fine without real config, but a minimal app launching servers directly can just omit them.
- [nav2] gotcha (run 4): TB3's burger.yaml uses `$(find-pkg-share nav2_bt_navigator)/behavior_trees/....xml` substitutions in bt_navigator params. nav2_bringup expands these via `ParameterFile(RewrittenYaml(...), allow_substs=True)`; if you launch nav2 servers yourself and pass the YAML path straight into `Node(parameters=[...])`, bt_navigator receives the LITERAL `$(find-pkg-share ...)` string → `Couldn't open input XML file` at activation → lifecycle manager aborts bringup. Fix: `launch_ros.parameter_descriptions.ParameterFile(path, allow_substs=True)`. Belongs next to any "launch nav2 servers directly" guidance in the nav2 skill.
- [nav2] gotcha (run 5, checklist rung 5 goal): with slam_toolbox the `map` frame origin is the robot's STARTING pose, not the world origin — goals written in Gazebo world coordinates are silently wrong by the spawn offset. Symptom: `GridBased plugin failed to plan from (0.00, -0.00) to (-1.70, 0.00): "Goal Coordinates ... was outside bounds"` (robot spawned at world (-2.0,-0.5) = map (0,0); every world-frame ring goal landed outside the young costmap). Fix: goals_map = goals_world - spawn_pose. Follow-on for M3: the SAVED map inherits this origin, so nav-on-saved-map goals need the same shift (or pick goals from the map itself in Foxglove). The nav2 skill's SLAM examples never mention the map-origin-at-start convention — worth one line.
- [nav2]/[gazebo] route-design lesson (run 6, 5/9): the brief's mapping octagon r=1.7 put its diagonal waypoints (±1.2,±1.2) 0.14 m from turtlebot3_world pillar CENTERS (pillars r=0.15 at {-1.1,0,1.1}² — i.e. goals inside the obstacle). North-side ones "succeeded" only because the area was already mapped and navfn's 0.5 tolerance rescued them; the first south-side one (unknown space at that point) failed `"Failed to create plan with tolerance of: 0.500000"`, and the ensuing spin/backup recovery loop smeared the SLAM map around the robot until EVERY plan from that pose failed (even back to previously-reached free cells; costmap clears didn't help because the static layer refills from the corrupted /map). Fix: waypoints with real clearance — outer axis points (±1.7,0),(0,±1.7) alternating with inner diagonal midpoints (±0.55,±0.55), all ≥0.4 m from obstacles. Lesson for nav2 skill's waypoint/SLAM content: check goal clearance against the actual world geometry (grep the SDF pillar poses), and know that repeated failed-goal recovery spins degrade a live SLAM map.
- [nav2] `waitUntilNav2Active(localizer='slam_toolbox')` worked exactly as the skill reference implies ✓ — with slam_toolbox launched via its own online_async_launch.py (lifecycle-managed, auto-activating) BasicNavigator waited for /slam_toolbox + /bt_navigator to hit active and returned; no initial pose needed in SLAM mode. Caveat from runs 2-4: it returns as soon as those two nodes are active, which can be BEFORE the rest of the stack is truly usable — it gives no protection against a bond-shutdown or unconfigured planner a second later.
- [nav2] map_saver_cli worked as documented ✓: `ros2 run nav2_map_server map_saver_cli -f /ws/maps/map --ros-args -p use_sim_time:=true` against live slam_toolbox /map wrote map.pgm+map.yaml in one shot, and the yaml's `image:` came out RELATIVE (`image: map.pgm`) on jazzy — the brief's warning about absolute paths didn't materialize.
- [nav2] slam_toolbox online_async defaults on jazzy already ship `base_frame: base_footprint` and `scan_topic: /scan` ✓ — only `use_sim_time: true` needed adding for the TB3 sim case (the skill/brief expectation that base_frame needs editing is stale for jazzy).
- [nav2] mapping run result (M2 done): 9/9 waypoints SUCCEEDED first-attempt with the clearance-aware route, full ring ~93 s sim time at RTF≈1; saved map 111x103 @0.05 m — free 7827 / occupied 746 / unknown 2860 px. Note map origin in map.yaml is (-0.953,-2.074) i.e. relative to SLAM start, NOT the gz world frame — M3 goals must use map-frame coords (world - spawn(-2.0,-0.5)).
- [testing] check_map.py free-threshold bug worth remembering: ROS trinary PGM uses free=254, UNKNOWN=205, occupied=0; a "free >= 0.75*maxval" check counts unknown gray (205 > 191) as free, making an all-unknown map pass the free-space assertion. Threshold must sit above 0.80*maxval (used 0.9).
- [nav2] AMCL-on-saved-map worked as the skill documents ✓ (Task 6/M3): BasicNavigator `setInitialPose(map (0,0))` + `waitUntilNav2Active()` (default localizer 'amcl') localized first try — no covariance tuning, delays, or retries needed; Nav2 ready ~5 s after send_goals started. One benign one-shot warn worth knowing: the FIRST initial-pose publish can race sim time — `[amcl] Failed to transform initial pose in time (Lookup would require extrapolation into the future. Requested time 41.686000 but the latest data is at time 41.680000, ... base_footprint to odom)` — harmless because `waitUntilNav2Active`'s `_waitForInitialPose` loop republishes until /amcl_pose arrives.
- [nav2] pre-initial-pose log storm is NORMAL, looks alarming: between `nav.launch.py` up and send_goals, planner_server's global_costmap spams `Timed out waiting for transform from base_link to map ... Invalid frame ID "map"` every 0.5 s and AMCL warns `AMCL cannot publish a pose or update the transform. Please set the initial pose...` every ~2 s. Neither is an error — the map frame simply doesn't exist until AMCL gets its initial pose. Candidate one-liner for nav2's AMCL/bringup section (saves a false-alarm debug loop).
- [nav2] map_server relative-path gotcha handled as predicted: TB3 burger.yaml ships `yaml_filename: "map.yaml"` (relative → resolved against node cwd → LoadMap fails). Fix that worked: per-node params override AFTER the ParameterFile — `parameters=[ParameterFile(params_yaml, allow_substs=True), {'yaml_filename': abs_map_path}]` — later entries win; no YAML editing or RewrittenYaml needed. Pairs with the direct-server pattern already logged.
- [nav2] R3 CLOSED at runtime (M3 done): full Nav2 stack on the saved map with `enable_stamped_cmd_vel: true` in all 5 cmd_vel sections drove the real TB3 burger sim end-to-end — both map-frame goals (3.7,0.5) and (0.3,0.5) `TaskResult.SUCCEEDED` on the FIRST run, `PASS: all goals reached`, exit 0, ~27 s wall total at RTF≈1; final /amcl_pose (0.279,0.464) ≈ goal within the 0.25 xy tolerance, physically confirming stamped cmd_vel moves the robot under Nav2 (not just under manual pub as in Task 3).
- [nav2] single lifecycle manager for localization+navigation works (vs nav2_bringup's two managers): map_server + amcl simply listed FIRST in the one manager's node_names (map/localization exist before costmaps activate) — same bond_timeout 0.0 helper as slam.launch.py, no ordering issues observed.
- [testing] smoke-test shape worked as the skill's trial-run bar describes ✓ (Task 7): one command (`make smoke`) → compose `--profile test up --build --abort-on-container-exit --exit-code-from smoke` → `run_smoke.sh` (launch in bg, outer `timeout -k 10 ${SMOKE_TIMEOUT:-180}` around the goal client, bounded shutdown tail, `exit ${RC}`) went green TWICE consecutively: `PASS: all goals reached`, `SMOKE RESULT: 0`, `exit=0` — 94 s and 82 s wall including image-rebuild check and container teardown. Exit-code propagation through all five layers (python `sys.exit` → `timeout` → script → container exit code → compose `--exit-code-from` → make) worked exactly as documented, zero friction. Outer-timeout-wraps-the-whole-run is the load-bearing detail: send_goals' own `--timeout` clock starts AFTER `waitUntilNav2Active()`, so without the outer `timeout(1)` a never-activating stack is an unbounded hang, not a red test.
- [integration] compose gotcha (Task 7, adding SMOKE_TIMEOUT passthrough): YAML merge keys (`<<: *anchor`) are SHALLOW — declaring `environment:` on a service that merges an anchor REPLACES the anchor's whole environment map (silently dropping ROS_DOMAIN_ID/TURTLEBOT3_MODEL) rather than merging into it; every anchor env var must be redeclared alongside the new one. Worth a line in integration's compose patterns.
- [environments] clean-room verification ✓ (Task 8): `docker compose --profile "*" down --rmi local --remove-orphans` (image confirmed removed) → `make smoke` → rebuild + full smoke scenario green in 96 s total, exit 0 — the committed repo alone reproduces the pass bar. Caveat for the skill's parity checklist: `down --rmi local` removes the image but NOT the buildx layer cache, so this run re-exercised the committed build definition against cached (content-checksummed) layers; a truly cold rebuild additionally needs `docker builder prune`. The parity bar as stated ("same commands, same result") held.

## End-of-block retro — nav-trial build (2026-07-10/11), one line per loaded skill

- [architect] fired ✓ (explicit at kickoff, quiet after) · accurate ✓ (Jazzy-exception + issue ref verified live; golden path correct end-to-end) · complete ✓ at routing altitude (leaf gaps belong to tool skills) · lean ✓
- [environments] fired ✓ (loaded right after architect, per its own directive) · accurate ✓ (Dockerfile.ros2 shape + macOS-no-native-ROS gotcha both load-bearing) · complete − (parity checklist lacks the buildx-cache caveat: `down --rmi local` ≠ cold rebuild) · lean ✓
- [gazebo] fired ✓ · accurate ✓ (headless-rendering flag, Jazzy↔Harmonic pairing, gz-in-Docker deferral all correct) · complete − (headless section should warn that upstream robot demo launches often hardcode a GUI client — TB3 did; nothing on vendored `gz` CLI needing a sourced env) · lean ✓
- [nav2] fired ✓ · accurate ✓ (bringup patterns, AMCL-silent gotcha, and the won't-move checklist which caught real failures at rungs 2/5/6 during SLAM debugging) · complete −− (biggest gap set of the block: TwistStamped/enable_stamped_cmd_vel, SLAM map-origin-at-start-pose convention, direct-server launch needs ParameterFile(allow_substs=True), jazzy bringup slam:=True double-slam_toolbox + unsettable bond_timeout) · lean ✓
- [ros2] fired ✓ · accurate ✓ (rosdep-first, package anatomy, sourcing order) · complete − (launch_testing: testing routes here, nothing exists) · lean ✓
- [foxglove] fired ✓ · accurate ✓✓ (worked exactly as documented on first real use — browser + ws://localhost:8765, zero extra config) · complete ✓ (nice-to-have: "bridge unreachable ≠ bridge broken, check the container runtime" gotcha) · lean ✓
- [integration] fired ✓ · accurate ✓✓ (macOS DDS-multicast gotcha drove the whole one-container-per-scenario design, verified end-to-end) · complete − (compose gotchas found under load: profile-gated `build` no-op, YAML merge-key shallowness, `exec` bypasses ENTRYPOINT) · lean ✓
- [testing] fired ✓ · accurate ✓ (trial-run bar + smoke shape worked verbatim; exit-code chain exactly as framed) · complete − (launch_testing routing dead-end shared with ros2) · lean ✓
