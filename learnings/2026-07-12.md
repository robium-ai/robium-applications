# 2026-07-12 — manip-trial

- [architect] fired ✓ on explicit kickoff invocation; manipulation golden path (LeRobot + uv, Isaac GPU-gated out, macOS-no-Isaac gotcha) matched this build's constraints exactly — requirement checklist covered everything the README trial spec needed, no missing items to ask about
- [lerobot] stale version fact (found by robium-architect agent during brief research): skill states LeRobot is at **0.6.1**, but PyPI (JSON API + simple index, checked 2026-07-12) tops out at **0.6.0**, released 2026-07-06. Brief pins 0.6.0; all CLI/flag/extras facts re-verified at the v0.6.0 tag instead of trusting the skill
- [environments] decision-tree edge case not covered: brief's stack is pure-Python EXCEPT one system dep (ffmpeg for torchcodec video decode). Skill's Customization section says "adding system packages to a uv-only project → graduate to Docker, not host apt/brew" — but on macOS Docker would lose the MPS GPU (skill's own gotcha: GPU containers are Linux-only), so uv + `brew install ffmpeg` is the only strategy that keeps Apple-silicon acceleration. Candidate nuance: "unless the system dep is trivial and Docker would cost the accelerator (macOS/MPS), document the single host step instead"
- [lerobot] figured out from scratch (first eval run): `lerobot-eval` defaults `--eval.use_async_envs=true`, and lerobot 0.6.0 builds AsyncVectorEnv with `context="forkserver"` (envs/configs.py) — forkserver children never import `gym_pusht`, so every worker dies with `gymnasium.error.NamespaceNotFound: Namespace gym_pusht not found` and the parent surfaces only `BrokenPipeError` in `_check_spaces` (real error buried mid-log). Fix verified: `--eval.use_async_envs=false` (SyncVectorEnv). Platform-independent by the look of it (forkserver children are fresh everywhere); candidate gotcha for lerobot's eval-and-sim reference AND its Quick start eval example, which omits the flag
- [lerobot] wrong/incomplete extras in Quick start: step 1 installs `lerobot[core_scripts,training,pusht]`, step 3 evaluates `lerobot/diffusion_pusht` → `ImportError: 'diffusers' is required but not installed` at policy load (lerobot 0.6.0 moved diffusion deps behind a `diffusion` extra). The skill's own quick-start sequence fails as written; eval of any diffusion checkpoint needs `lerobot[diffusion]`. Also: `core_scripts` is NOT needed for the train+eval sim path (train/eval/dataset-viz CLIs all present without it — verified this session)
- [lerobot] "start from pretrained" directive is broken on 0.6.0 with its own example checkpoint: `lerobot/diffusion_pusht` (Hub main, last modified 2025-03, still cited in lerobot_eval.py's v0.6.0 docstring AND the skill's Quick start step 3) lacks `policy_preprocessor.json`/`policy_postprocessor.json` → `FileNotFoundError: Could not find 'policy_preprocessor.json' on the HuggingFace Hub` at processor load. `make_pre_post_processors` has NO fallback when pretrained_path is set (verified in policies/factory.py); old-format checkpoints are simply un-evalable on 0.6.0. Brief's open risk 3 predicted this; pass bar unaffected (own ACT checkpoint). Candidate fix for the skill: warn that pre-processor-pipeline-era Hub checkpoints don't load on >=0.6, suggest checking the repo tree for policy_preprocessor.json before relying on one
- [lerobot] MPS speed gotcha looks badly pessimistic: skill says ACT on Apple Silicon takes "~6-14 h" for 5 epochs over a ~45k-frame dataset; measured on M2 Pro (32 GB, lerobot 0.6.0, torch 2.11): ACT on lerobot/pusht (25.6k frames, 96×96) runs 11.6 steps/s @ batch 8 (93 samples/s) → one epoch ≈ 5 min, 5 epochs ≈ 25 min; scaled to 45k frames ≈ 45 min — order of magnitude faster than claimed. 200-step smoke train: 31 s wall. Candidate: refresh the compute table's MPS row (or scope it to bigger-obs datasets)
- [lerobot] risk closed ✓: torchcodec 0.11.1 + Homebrew ffmpeg 8.1.2 on macOS arm64 decoded lerobot/pusht's AV1 videos fine during training (video_backend=torchcodec, zero decode errors) — the "torchcodec is strict about ffmpeg majors" worry did not bite at these versions
- [lerobot] checkpoint layout on 0.6.0 confirmed: outputs/.../checkpoints/000200/pretrained_model/{config.json,model.safetensors,policy_pre/postprocessor.json+safetensors,train_config.json} plus checkpoints/last pointer — the skill example's `checkpoints/last/pretrained_model` path is correct
- [lerobot] verified fact for the skill's eval reference: on 0.6.0, `eval_info.json` top-level schema is `{"per_task": [...], "per_group": {...}, "overall": {...}}` with the metrics (`avg_sum_reward`, `avg_max_reward`, `pc_success`, `n_episodes`, `eval_s`, `eval_ep_s`, `video_paths`) inside `overall` — older versions used an `aggregated` key; anything parsing eval output needs the new nesting
- [testing] policy-eval layer nuance missing: Quick start §3 prescribes "a handful of episodes with a stated success-rate threshold" as the manipulation smoke test — right for a regression gate over a known-good checkpoint, wrong for a train-from-scratch pipeline smoke (a 200-step ACT policy legitimately scores pc_success=0; a threshold would either always fail or be meaningless). manip-trial's smoke asserts exit codes + numeric metrics instead, per the trial pass bar ("training run completes; eval produces metrics"). Candidate: split the policy-eval row into pipeline-smoke (no threshold) vs regression-gate (threshold) sub-cases
- [testing] trial-run bar framing worked ✓: "policy-eval smoke run as the done bar" mapped 1:1 onto manip-trial (uv run pytest tests/test_smoke.py → 200-step train + 2-episode seeded eval, 2 passed in 39.5 s); pyramid routing (mechanics → lerobot, framing → testing) held with no dead ends this time
- [visualization]/[foxglove]/[lerobot] cross-vertical fact none of the skills mention: lerobot 0.6.0's `lerobot-dataset-viz` has `--display-mode {rerun,foxglove}` — Foxglove is now a first-class LeRobot dataset viewer (plus `--save 1 --output-dir` for a headless .rrd artifact, the CI-friendly path). The robium story ("foxglove = ROS/remote viz, rerun = ML logging") is missing this bridge
- [lerobot] figured out from scratch (baseline-eval debugging, systematic-debugging walk): there is currently NO working pretrained PushT baseline for lerobot 0.6.0 on the Hub. Official `lerobot/diffusion_pusht` can't load (missing processor files, above); community `ebenl08/diffusion_pusht_migrated` loads cleanly (sane normalizer stats: ImageNet-style image mean/std, correct 0-512 action min/max) but evals at chance level — pc_success 0%, avg_max_reward = initial-state coverage, bit-identical trajectories on cpu vs mps and 96 vs 384 obs (policy output effectively useless). Eval pipeline itself verified healthy by probing our own ACT checkpoint step-by-step: raw action ~N(0,1) → unnormalized to pixel coords → agent tracks target, rewards respond. Either the community migration mangled something subtle or 0.6.0 diffusion inference drifted from the checkpoint's era. Consequence for the skill: "evaluate lerobot/diffusion_pusht first to validate the pipeline" is dead on 0.6.0 — the working equivalent is "smoke-train your own tiny ACT and eval that"
- [lerobot] MPS eval note: pusht rollouts at 384×384 obs ran ~3.5 it/s (diffusion, batch 5) and 2-episode ACT eval took 3.6 s (batch 2, 96-ish workload) — eval on Apple Silicon is fully workable; issue #143-era float64/MPS failures did NOT reproduce on 0.6.0/torch 2.11 (cpu and mps produce identical rollouts)
- [lerobot] baseline pivot outcome (make train-baseline + baseline-eval): 10k-step ACT on lerobot/pusht, MPS, 14.5 min train (loss 14.4 → 0.33, 3.1 epochs) → 10-episode eval in 11 s: pc_success 0%, avg_max_reward 0.283 (vs 0.008 at 200 steps — policy demonstrably learns; PushT success needs ≥95% coverage, ACT only approaches that at ~100k steps). Useful skill fact: ACT-on-pusht is the fast-iteration pair but not a success-rate pair at smoke scale; anyone wanting nonzero pc_success on pusht needs diffusion-class training budgets
- [testing] pass-bar repeatability ✓: `make smoke` green twice consecutively (39.5 s, 43.0 s warm) — 200-step MPS train + 2-episode seeded eval + metrics assertions; exit-code chain pytest→make held

## End-of-block retro — manip-trial build (2026-07-12), one line per loaded skill

- [architect] fired ✓ (explicit kickoff; quiet after) · accurate ✓ (manipulation golden path LeRobot+uv correct end-to-end; Isaac/macOS gates load-bearing) · complete ✓ at routing altitude · lean ✓
- [environments] fired ✓ (loaded right after architect per directive) · accurate ✓ (uv-patterns shape used verbatim: pin, add, uv run, lock committed) · complete − (system-dep-means-Docker rule needs the macOS/MPS exception — Docker would cost the accelerator) · lean ✓
- [lerobot] fired ✓ · accurate −− (worst accuracy set of the block: version 0.6.1 vs PyPI 0.6.0; Quick start extras miss `diffusion` for its own step 3; "eval lerobot/diffusion_pusht first" directive dead on 0.6.0; MPS-slowness gotcha ~10× pessimistic for ACT/pusht) · complete − (async-env eval crash, eval_info.json schema, checkpoint layout all had to be figured out or verified live) · lean ✓ (structure itself was easy to navigate; train-act example template was the right shape and its checkpoints/last path correct)
- [testing] fired ✓ · accurate ✓ (trial-run bar drove the app shape; policy-eval-as-smoke framing worked) · complete − (needs pipeline-smoke vs regression-gate split for the threshold prescription) · lean ✓
