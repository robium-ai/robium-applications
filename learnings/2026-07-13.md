
- [live-demo] figured out from scratch (demo orchestrator build): the in-container gateway can't manage its own lifecycle (it dies with the container it would need to restart), and a browser can't start containers. The missing piece locally is a host-level **orchestrator** — a small always-on service (Node/TS + dockerode) that owns start/stop/list/limit across demos and hands the browser a per-instance connect host; the browser then talks to that instance's gateway directly (lifecycle-only, never in the data path). This is the exact role Cloud Run's control plane plays in prod, and a clean `Driver` interface lets LocalDockerDriver (now) and a future CloudRunDriver share the API. Verified end-to-end: page Start spawns a fresh container on an ephemeral port → sim ready rtf 1.2 → Stop removes it, zero manual `make demo`. Strong candidate for a live-demo skill architecture reference (the gateway-vs-orchestrator split + the local/cloud driver seam). <!-- absorbed: 2026-07-13 -->

# 2026-07-13 — demo v4 IDE workspace + orchestrator (gotchas)

- [live-demo] figured out from scratch (IDE Console pane): a real in-browser terminal into a demo container = xterm.js on the client + a **PTY over WebSocket** on the gateway. No ws library needed inside the container — hand-roll the RFC6455 frame codec (mask/unmask, opcodes) in ~40 lines of stdlib; `pty.fork()` + `os.execvp('bash')` + pipe bytes both ways. Bridge subprotocol note still applies (foxglove.sdk.v1). Candidate content: live-demo skill's IDE/terminal reference. <!-- absorbed: 2026-07-13 -->
- [live-demo] security shape for a public shell (deployed but verification deferred): a real shell into a Cloud Run container is defensible ONLY with (1) a dedicated **zero-IAM-role service account** (a stolen metadata token then unlocks nothing — which forced moving the fleet-count query off the demo container) and (2) **VPC deny-all egress** (Direct VPC egress + a deny-all firewall so the container can't dial out; the sim needs no egress — all traffic is the inbound visitor ws). Cloud Run gen2 microVM isolation handles container-escape; these two handle the actual holes (creds + egress). See robium.org docs/BACKLOG.md for the pending egress-block E2E verification. <!-- deferred: 2026-07-13 — fails promotion bar: egress-block E2E verification still pending (robium.org BACKLOG); revisit once verified -->
- [live-demo] Cloud Run Direct VPC egress needs a subnet big enough for the instance pool: a `/28` (16 IPs) failed deploy with "no sufficient IP addresses in the VPC network" at max-instances=5 + 8 vCPU; a `/24` fixed it. `gcloud compute networks subnets expand-ip-range` widens in place. <!-- absorbed: 2026-07-13 -->
- [live-demo]/[integration] CONCURRENT sims must not share a ROS_DOMAIN_ID. The moment an orchestrator makes >1 sim run at once, hardcoding one domain (42) makes their ROS graphs merge over the Docker network → two Gazebo `/clock` publishers → `[robot_state_publisher] Moved backwards in time, re-publishing joint transforms!` floods forever. Fix: assign the lowest-free ROS_DOMAIN_ID (1..200) per instance in the driver, label it, exclude used ones. (gz-transport side is already isolated by GZ_RELAY=127.0.0.1 = loopback-only, per-container.) Candidate: live-demo skill + a note in the orchestrator pattern. <!-- absorbed: 2026-07-13 -->
- [live-demo] frontend gotcha with dynamic backends: once Start picks a per-instance host (ephemeral port), a `setInterval(poll)` closure captures the STALE host and polls the wrong backend forever (symptom: logs stream fine via a re-rendered child, but the ready-state/Foxglove-button never flips). Fix: poll reads a `hostRef.current`, not the `host` closed over at interval-creation. General lesson for any UI whose backend address changes mid-session. <!-- absorbed: 2026-07-13 -->
- [live-demo] local-dev ergonomics that every demo will want: (1) a `?host=`/switcher to point a HMR frontend at prod-cloud OR a local backend; (2) same-site subdomain (demo.<domain>) + credentialed fetch + localhost-CORS so Cloud Run's affinity cookie actually routes; (3) cloud-disposable Stop/close semantics are WRONG for a warm local container — but the real fix is the orchestrator (Stop truly removes, Start truly spawns, everywhere) rather than special-casing localhost; (4) bind `npm run dev` to launch site + orchestrator together (concurrently). These are the difference between a demo you can iterate on and one you fight. <!-- absorbed: 2026-07-13 -->

# 2026-07-13 — vla-trial (design session)

Design/brainstorm block only — no build yet. Entries below are therefore mostly
**figured-out-from-scratch** research findings, not battle-tested build scars. Where an entry
has no verifying check yet it says so; those stay tentative until M0/M1 confirms or kills them
(evidence bar, CLAUDE.md).

## Skill-firing signals

- **[architect] / [none] the entry-point skill did NOT fire on a new-app kickoff.** Exact
  phrasing that started the block: `"lets do demo 1"`, then the pivot: *"i actually meant to
  have ml vla based arm control here… lets decide the simulator, model, robot and how to
  visualize"*. That is textbook `architect` territory — its own description claims "starting any
  new robotics app" and "which robotics stack" — and nothing triggered. What ran instead:
  `superpowers:brainstorming` (process skill) + an ad-hoc `general-purpose` agent I hand-wrote a
  6-question research brief for. The `robium-architect` agent, whose entire job is "heavy
  stack-selection research → architecture brief", was never invoked. Two separable problems:
  1. **Trigger surface** — `architect` looks keyed to greenfield phrasings ("build a robot app",
     "scaffold a robotics project"). It missed *"do demo 1"* / "demo N from the backlog", which
     is how an app actually starts **in this repo**. Verbatim eval case: `"lets do demo 1"`.
  2. **Entry-point collision** — `superpowers:using-superpowers` says process skills come first
     and "Let's build X" → brainstorming; robium's `architect` says *it* is the entry point and
     to "load it first". Both cannot be first. Brainstorming won, and the stack research got
     done by a hand-rolled agent duplicating what `robium-architect` exists to do. Needs an
     explicit hand-off rule (likely: brainstorming owns requirements, architect owns stack
     selection, brainstorming calls it).
  Highest-value entry in this file.
- **[lerobot] did not fire across an entire LeRobot session.** The whole block was
  LeRobot / SmolVLA / LeRobotDataset / fine-tuning and the skill never triggered. Every LeRobot
  fact below came from research, not the skill. Same likely cause: the ask was phrased as "vla
  based arm control", never "lerobot".
- **[huggingface] did not fire on a Gradio hosting question.** User, verbatim: *"if we use
  gradio do we need to host it on hugging face? can we just host it on our server and demo
  window would show the application?"* Nothing triggered. Common misconception, short stable
  answer — strong skill candidate (content below).
- **[superpowers:brainstorming] worked as documented ✓.** One-question-at-a-time →
  propose-approaches → design → spec held up under a mid-session pivot that invalidated the
  original premise. Its HARD-GATE is what stopped me scaffolding a MoveIt app the user did not
  want.

## Figured out from scratch — VLA model selection

- **[lerobot] / [none] no VLA model-selection guidance exists in any skill, and the choice is
  nearly forced once the numbers are on the table.** Compiled by hand:

  | Model | Params | Runs without a GPU? | Evidence |
  | --- | --- | --- | --- |
  | SmolVLA (`lerobot/smolvla_base`) | 450M | **Yes** (HF: "CPU… or even a MacBook") | HF blog, arXiv 2506.01844 |
  | pi0 / pi0-FAST (openpi) | ~3B | No — >8 GB VRAM; openpi repo is **Ubuntu-only** | openpi README |
  | MolmoAct2 | 5.5B | No (see below) | model card |
  | OpenVLA | 7B | No — ~14 GB VRAM | — |
  | GR00T N1.5/N1.7 | 2–3B | No — needs flash-attn + CUDA 12.4 | NVIDIA/Isaac-GR00T |

  SmolVLA is the only member of the "runs on a laptop" class. Its pretraining corpus is SO-100
  data *exclusively* (487 community datasets, ~10M frames) — so an SO-100/101 fine-tune is
  **in-embodiment**. Consequence worth stating in the skill: **the arm choice and the model
  choice are one choice**, not two.
- **[none] "zero-shot" on a VLA model card means zero-shot on the *embodiment*, not on your
  *renderer*.** Cost ~an hour. `allenai/MolmoAct2-SO100_101` is real (5.4k downloads) and
  genuinely zero-shot on SO-100/101 — but (1) it is **5.49B params, CUDA-only in practice**
  (every documented path is `.to("cuda")` with `enable_cuda_graph=True` *by default*; ~26 GB
  fp32 / "under 16 GB" bf16), and (2) it was trained on **RealSense images of real scenes**
  (sample inputs are literally `sample_realsense_top_rgb.png`), so feeding it MuJoCo renders is
  a real→sim domain gap in the unfavourable direction. Not falsified by a run (we declined to
  rent a GPU) — **tentative** — but the memory numbers alone settle it for a laptop demo.

## Figured out from scratch — MuJoCo on macOS (feeds the unwritten `mujoco` skill)

- **`MUJOCO_GL=cgl` is *the* macOS headless-render backend.** `osmesa` and `egl` are
  **Linux-only** (mujoco#2164 is the open port request). Without this there is no obvious way to
  render policy camera observations without a display on a Mac — a hard blocker, silently.
- **The interactive viewer (`viewer.launch_passive`) needs `mjpython`, not `python`,** on macOS
  (main-thread rendering constraint); also a known bug under `uv` (mujoco#1923). Guaranteed
  first-run stumble.
- **Version-pin trap:** `gym-hil`/`gym-aloha` pin `mujoco<3.9`, `gym-xarm` pins `<3.0`, but
  `mujoco_menagerie`'s SO-101 needs **≥3.1.3** and current MuJoCo is **3.10**. A hard dep on
  `gym-hil` silently *downgrades* MuJoCo below the model's own requirement. Copy its patterns;
  don't depend on it.
- **Offscreen render throughput on M-series is unmeasured** (upstream: "Offscreen rendering with
  mjr_render is extremely slow"). M0 spike, not yet answered.

## Figured out from scratch — robot asset

- **[none] SO-101 is in `mujoco_menagerie`** (`robotstudio_so101/`) and ships `scene_box.xml`, a
  pick-and-place-ready scene, with **manipulation-tuned collision geometry and a camera mount**
  already added. **Do NOT use the upstream `TheRobotStudio/SO-ARM100` MJCF** — its own README
  admits the gripper linear-joint mapping "is not yet reflected in the current URDF and MuJoCo
  files." Picking the wrong one of these two costs days.
- **No SO-101 Gymnasium env exists** in any maintained package (`gym-hil` is Franka-only:
  `PandaPickCube*-v0`), and the one ready-made *language-conditioned* MuJoCo benchmark, LIBERO,
  is **Linux-only** per LeRobot's own docs. So a language-conditioned SO-101 sim env must be
  hand-built. **This is the largest hidden cost in any "just fine-tune a VLA in sim" plan** and
  nothing in the skills warns about it.

## Figured out from scratch — training economics

- **[lerobot] MPS fine-tuning is a non-starter, and the skills say nothing about it.** LeRobot
  *supports* `--policy.device=mps`, which actively invites you to try. Reported data point:
  **~2 hours to complete 20 steps.** A fine-tune is 20,000. MPS is for inference and for proving
  the loop starts — never for training. Deserves a loud line in `lerobot` and/or `environments`.
- **[lerobot] LeRobot 0.6.0 made remote training one flag:** `lerobot-train
  --job.target=a10g-small …` pushes the dataset to the Hub if needed, submits to HF Jobs, and
  streams logs back to the terminal. That is the entire local-Mac / remote-GPU story with **zero
  infra to build** — exactly what `environments` claims to care about. Costs: **20k steps ≈ 4 h
  on an A100** (HF docs); an independent practitioner run (75 eps, 20k steps, batch 64) took
  **~10.4 h on a 3090 → 60–80% success**. So ~$5–15 per fine-tune.
- **[data] / [none] episode *density* beats episode *count*, and the failure boundary is
  documented but obscure.** 25 eps → bad. **50 eps over a 30 cm workspace → outright failure**
  ("learned the general motion but couldn't pin down grasp locations"). **75 eps over ~10 cm →
  80% success.** Anyone planning "~50 episodes" — the number that feels natural, and the number
  I first proposed — is sitting exactly on the wrong side of the boundary. Candidate: a dataset
  sizing rule in `data` or `lerobot`.
- **LeRobot 0.6.0 requires Python ≥3.12** — silently breaks 3.10/3.11 envs.

## Figured out from scratch — UI / viz

- **[huggingface] Gradio has NO HuggingFace dependency, and users assume it does.** It is a plain
  Python web app (FastAPI + uvicorn, :7860). Three self-hosting mechanics worth one short skill
  section: (1) `gr.mount_gradio_app(app, io, path="/ui")` mounts it into an **existing FastAPI
  app** — one process, one port (this is what lets a demo gateway host the UI with no second
  service); (2) nginx subpath reverse-proxy — needs the WebSocket `Upgrade`/`Connection` headers
  and `proxy_buffering off`; (3) embed anywhere via `<iframe>` or the `<gradio-app src=…>` web
  component (lazy-loads, auto-height) — and **`src` can be any URL, it does not have to be
  `*.hf.space`**. HF Spaces is one deployment target for a Gradio app, not a prerequisite.
  Verified against current docs (Gradio 6.0.1) via ctx7; not yet run by us — tentative until M4.
- **[rerun] / [huggingface] `gradio_rerun` exists** — an official Rerun-maintained Gradio custom
  component (`rerun-io/gradio-rerun-viewer`; PyPI `gradio_rerun` **0.34.1**; requires
  `gradio>=6.0.0` and `rerun-sdk==0.34.1`). That rerun pin **matches robium's `rerun` skill pin
  exactly**. So Rerun-in-Gradio is a *component*, not two embeds fighting for the same panel — a
  materially better design that neither skill mentions. Strong candidate for both.
- **[rerun] for a VLA demo Rerun beats a video stream on merit, not brand.** A plain MJPEG
  rectangle discards joint states, the language instruction, and the predicted action chunk.
  Rerun puts camera + 3D scene + scalars + instruction + chunk on one scrubable timeline — which
  *is* the explanation of what the VLA did. The skill currently undersells this; it is the
  reason-to-choose.

## Notes / process

- **[environments] Docker on macOS cannot see MPS — this decides architectures, and no skill says
  so. (seen 2x)** manip-trial's registry card already records it (it uses `uv` *because* "a
  container would lose MPS"). New consequence found today: for a containerized VLA demo the
  number that decides the architecture is **CPU** inference latency, not MPS — because the
  container is CPU on a Mac *and* CPU on Cloud Run. Candidate: a decision-rule line in
  `environments` beside the existing uv-vs-Docker rule.
- **User-corrected approach, twice, both toward honesty about scope** — rejected a live on-page
  fine-tune in favour of switchable before/after checkpoints, and ruled out macOS training
  outright. Both corrections matched what the evidence independently showed afterwards.

# 2026-07-13 — vla-trial Task 3 (M0 policy spike — build block)

- **[lerobot] figured out from scratch: `select_action()` does NOT tokenize the task string — the batch must be run through the policy's own pre-processor pipeline first.** The natural reading of the API (`policy.select_action(batch)` with `batch["task"] = "put the cube in the bin"`) fails with `KeyError: 'observation.language.tokens'` at `modeling_smolvla.py:290`. The tokenization/normalization/batching live in a *separate* pipeline built by `make_pre_post_processors(policy_cfg=..., pretrained_path=...)` (`lerobot.policies.factory`, re-exported from `lerobot.policies`), which you call on the raw obs dict before `select_action`. Verified: with the preprocessor, a forward pass returns a `(1, 6)` action tensor; without it, `KeyError` every time. Dead end ruled out: passing `task` as a list vs a string changes nothing — the key is missing either way, it is not a shape bug. Gotcha #2 in the same call: `make_pre_post_processors` reads `device` from the *checkpoint's* config, which is `cuda`; on a Mac it raises `AssertionError` inside `get_safe_torch_device` unless you pass `preprocessor_overrides={"device_processor": {"device": device}}`. Both are invisible in the model card. High-value for the `lerobot` skill.
- **[lerobot] the pretrained `smolvla_base` checkpoint declares GENERIC camera keys, not your env's.** `policy.config.image_features` is `observation.images.camera1/2/3` (each `(3,256,256)`), NOT `observation.images.wrist`/`.scene`. Passing your own names raises `ValueError: All image features are missing from the batch. At least one expected.` — a confusing message, since the batch is full of images; it means *none of the names matched*. Build benchmark batches from `policy.config.image_features` / `.robot_state_feature` rather than hardcoding, and treat env→policy camera remapping as an explicit step (LeRobot has a `rename_map` / `RenameObservationsProcessorStep` for exactly this).
- **[lerobot]/[environments] MEASURED: SmolVLA forward-pass latency on Apple Silicon (M-series), 450M `smolvla_base`, 20 timed passes, 5 warm-up, `policy.reset()` before every timed call.** No published benchmark for this existed anywhere; HF's "runs on a MacBook" is a marketing claim. **MPS 0.536 s mean / 0.543 p95. CPU native 9.186 s / 9.399. CPU in a linux/arm64 container 9.318 s / 9.621.** Two conclusions the skills should carry: (1) **MPS is 17x faster than CPU for VLA inference** — "runs on a MacBook" is true *only* with MPS; on CPU it is ~9 s/chunk, which is not a demo, it is a slideshow. (2) **Containerising costs ~nothing (+1.4%) — the penalty is losing MPS, not the container.** Since Docker-on-macOS cannot see MPS and Cloud Run has no GPU, any containerized/deployed VLA demo is on the 9 s path. This *confirms and quantifies* the existing "(seen 2x)" note that Docker-on-macOS loses MPS and that CPU is the number that decides the architecture. **(seen 3x)**
- **[lerobot] action chunking makes a naive benchmark report a fantasy number, and you have to know to defend against it.** SmolVLA's `select_action` serves ~50 subsequent calls from a cached chunk (`self._queues[ACTION].popleft()`), so timing it in a loop without `policy.reset()` measures a deque pop, not a forward pass. Evidence the guard works: with `reset()` every timed call costs ~9.2 s on CPU, and the spread is tight (8.99–9.65 over 20 passes) — real compute, not a cache. The trap is that the un-guarded version *looks* like a spectacular result rather than an error. Worth a warning in the `lerobot` skill and in `testing` (benchmarking a cached API).
- **[testing] a benchmark harness that swallows exceptions can report success while measuring nothing.** The dispatcher pattern `for device in devices: try: bench(device) except Exception as e: print(f"{device}: unavailable ({e})")` then `return 0` — which is what the task brief prescribed — let `make spike-policy-container` exit **0** while producing **zero** measurements (the container hit a transient HF Hub `401 Unauthorized` from `cas-server.xethub.hf.co` fetching weights). A measurement command must fail loudly when it measured nothing; fixed by counting successes and returning 1 if none. General rule for `testing`: **for a benchmark, "no data" is a failure, not a quiet skip.**
- **[testing] a spike that writes results keyed by device silently self-clobbers when the same device is measured in two runtimes.** `outputs/spike/policy.json` keyed on `"cpu"`, plus a container that bind-mounts the same `outputs/`, meant the container-CPU run **overwrote the native-CPU run** — destroying exactly the two numbers the spike existed to compare. Caught only because the native number was still in a scratch log. Key results by *(device, runtime)*, not device.
- **[environments] `docker build` of a torch-based image on linux/arm64 pulls the full NVIDIA CUDA wheel set** (`nvidia-cudnn-cu13`, `nvidia-nccl-cu13`, `triton`, …) even though the image is CPU-only and arm64 — a 3.46 GB image and a multi-minute build for a benchmark that never touches a GPU. Installing the CPU-only torch index (`--index-url https://download.pytorch.org/whl/cpu`) would cut this substantially. Not fixed here (the fat image measures the same thing), but any real deploy of this container should. Candidate: an `environments` note on torch-in-Docker image bloat.
- **[none] container re-runs re-download ~1 GB of weights every time unless the HF cache is mounted.** `-v $HOME/.cache/huggingface:/root/.cache/huggingface` makes container spike runs fast and repeatable — and it is what stopped the transient HF 401 from recurring. Obvious in hindsight; cost two failed runs.
- **[testing] the same clobbering bug bit a THIRD time, from the test suite — `make test` silently destroyed the M0 benchmark. (seen 3x in one task)** The brief's test called `bench_policy(device="cpu", n_passes=3)` with no output redirection, so a throwaway 3-pass smoke run wrote straight into the canonical `outputs/spike/policy.json` and **overwrote the real 20-pass benchmark**. Caught only by re-reading the artifact after commit and noticing `n=3` where `n=20` should be. Generalizable rule worth putting in `testing`: **a test must never write to a real output artifact.** Any bench/measure function needs an injectable output path, the test passes `tmp_path`, and — the part that actually holds the line — a regression test asserts the canonical artifact is byte-identical before and after a test run. Three variants of "shared mutable output path" in one task (container-vs-native, test-vs-real) says this is a systemic design smell in spike harnesses, not bad luck.

# 2026-07-13 — vla-trial (M0 spike: measured, not assumed)

The design-session entries above were tentative pending M0. These are the measurements.
Both of the spec's "explicitly unverified" claims are now **resolved**.

- **[lerobot] / [environments] SmolVLA on Apple Silicon — first benchmark we can find anywhere.**
  `lerobot/smolvla_base` (450M), 20 timed passes, 5 warm-up, `policy.reset()` before every call
  (see the chunk-cache gotcha below), `torch.mps.synchronize()` around the timed region:

  | runtime | mean | p95 |
  | --- | --- | --- |
  | **MPS native (M-series)** | **0.549 s** | 0.571 s |
  | CPU native | 9.004 s | 9.244 s |
  | CPU in linux/arm64 container | 9.318 s | 9.621 s |

  **The load-bearing finding: containerizing costs ~+3%. The 17x penalty is LOSING MPS, not the
  container.** HF's "runs on a CPU… or even a MacBook" is true only in the "it completes" sense —
  on CPU a forward pass is **9 seconds**. Anyone planning interactive VLA control on CPU is
  planning a slideshow. Consequence for robium: the uv-vs-Docker decision rule in `environments`
  is not just about convenience on Apple Silicon — for VLA inference it is a **17x** decision.
  Candidate: hard numbers in `lerobot` + a decision line in `environments`.

- **[lerobot] action chunking silently destroys naive latency benchmarks.** SmolVLA returns a
  chunk (~50 actions) and serves later `select_action()` calls from that cache. Time it in a loop
  without `policy.reset()` and you measure a **dict lookup**, report a wonderfully fast number,
  and it is meaningless. Corollary that saves the demo's architecture: chunking is also why ~1 s
  inference is *enough* — one pass buys ~1.7 s of robot motion at 30 FPS, so the bar is ~1 pass/sec,
  not 30 Hz. But it does NOT rescue CPU: a chunk costs 9.3 s and buys 1.7 s, so the executor
  starves 5.5x faster than the producer refills. **Async chunk-ahead hides latency, never a
  throughput deficit.**

- **[none] Benchmarking on MPS without `torch.mps.synchronize()` measures dispatch, not compute.**
  PyTorch MPS ops are async and LeRobot's `select_action` chain contains no `.cpu()`/`.item()`/
  `.numpy()` to force a wait — so nothing syncs implicitly. Caught in review, not by the
  implementer. Here it only inflated the result ~3% (0.536 → 0.549 s), but the same bug on a
  larger model would fake an arbitrarily good number. Any MPS/CUDA benchmark must sync **before
  starting and before stopping** the timer. Generic enough for `environments` or a testing note.

- **[lerobot] LeRobot 0.6.0: `SmolVLAPolicy.select_action()` does NOT tokenize.** Import path is
  as expected (`lerobot.policies.smolvla.modeling_smolvla.SmolVLAPolicy`), but feeding it a batch
  directly raises `KeyError: observation.language.tokens`. The batch must first go through
  `make_pre_post_processors(...)`, and you must pass
  `preprocessor_overrides={"device_processor": {"device": device}}` or it asserts on `cuda`.
  Also: the base checkpoint declares its image keys as `observation.images.camera1/2/3`, **not**
  semantic names like `wrist`/`scene` — build batches from `policy.config.image_features` /
  `.robot_state_feature` rather than hardcoding key names. Cost us a working bench on first try.

- **[testing] / [none] A benchmark that writes one shared artifact path will lie to you.** Three
  bugs of one class, all in code the plan itself prescribed: the container run overwrote the
  native result; `make test` overwrote the real 20-pass artifact with a 3-pass test one; and the
  CLI dispatcher caught per-device exceptions, printed them, and **exited 0 having measured
  nothing** (it did exactly this once, on a transient HF 401). Fixes: key the artifact by
  `device-runtime`, inject the output path in tests, and count successful measurements —
  `return 1` if zero. Lesson worth generalizing: **a measurement harness needs a regression test
  asserting it did not silently measure nothing.**

- **[gazebo]/[mujoco] MuJoCo offscreen rendering on Apple Silicon is NOT the bottleneck** —
  84.0–84.9 fps @256x256 via `MUJOCO_GL=cgl`, against a 60 fps floor (2 cameras × 30 Hz). Review
  also measured `mj_step` at **0.012 ms** vs **~11.8 ms/render** on this scene, so physics is
  effectively free and render cost dominates. Kills the "offscreen render is extremely slow"
  worry from the upstream issue tracker — at least for a low-DOF arm scene.

# 2026-07-13 — vla-trial Task 4 (SO-101 pick env)

- **[none] Figured out from scratch: `mujoco.Renderer` on macOS CGL is not bit-reproducible
  across camera modes/first calls — two separate, non-obvious bugs, both invisible until a
  test demanded pixel-exact determinism.** (1) A camera with `mode="targetbody"` re-runs
  MuJoCo's camera-tracking *smoothing filter* on every `update_scene()` call, so its rendered
  pose is call-history-dependent even when the underlying physics state (`qpos`, `xpos`,
  `cam_xpos`) is verified bit-identical between two `env.reset(seed=X)` calls — up to 85 LSB
  drift over 17% of pixels. Fixed by replacing `mode="targetbody" target="box"` with a static
  `xyaxes`-oriented camera computed once (look-at math), which uses plain forward kinematics
  like a `mode="fixed"` camera and is exactly reproducible. (2) Independently, a freshly
  constructed `Renderer`'s very first `(mj_resetData -> mj_forward -> render)` cycle is not
  repeatable relative to a *later* render of the identical state (~6 LSB drift, ~12% of
  pixels) — confirmed by rendering 8 consecutive identical-state frames: only the 0→1
  transition differed, every 1→2, 2→3, … onward was zero-diff. Fix: do one throwaway
  reset+render cycle in `__init__` (using the *real* reset path, not a raw pre-`mj_forward`
  render — that difference mattered: a naive warm-up render before any `mj_forward` call did
  NOT consume the "cold" cycle). Evidence bar: (a) failing check —
  `tests/test_env.py::test_seeding_is_deterministic` (`np.array_equal` on two same-seed wrist
  frames); (b) verbatim symptom — `assert False` with per-pixel RGB off by 1-6, e.g.
  `[52,77,103]` vs `[52,77,104]`, on ~12-17% of pixels, *not* random noise (background sky
  gradient pixels included); (c) dead ends ruled out — MSAA (`model.vis.quality.offsamples`)
  was a red herring (forcing it to 0 "fixed" the single-camera case but made the
  `targetbody` camera's own drift *worse*, 85 LSB vs 16); two independent fresh envs
  rendering the same state ARE bit-identical, which is what pointed at renderer-internal
  call-history state rather than GPU/driver randomness. Verified stable over 3 independent
  full pytest runs. Strong candidate for `mujoco`/`simulation` skill hardening: "camera
  `mode` other than `fixed` is not safe for reproducible offscreen rendering; warm up a new
  `Renderer` with one throwaway full reset+render cycle before trusting determinism."
- **[none] `solve_ik` (damped-least-squares, hand-written per the task-4 brief's instruction
  that it doesn't exist as an importable pure function) genuinely does not raise on an
  unreachable target — it just returns a locally-optimal qpos with the site short of the
  target.** Verified concretely: menagerie's own default box spawn `(0.5, 0, 0.03)` —
  the *vendored asset's own author's choice* — is **unreachable** by this SO-101 mount
  (`err=0.035 > tol=0.01`), confirming the brief's warning was not hypothetical. Also found:
  the originally-proposed bin position (`0.35 0.22 0.0`, half-width 0.055) failed reachability
  at its far `(+x,+y)` corner at lift height (`err=0.03`) even though its own center and table-
  height corners passed — **partial reachability is a real failure mode**, not just "reachable
  vs not" at a single point; every corner at every height that the oracle will actually need
  must be probed independently. Moved bin `pos` from `0.35 0.22 0.0` to `0.35 0.16 0.0` (only
  the y-coordinate changed); re-verified full footprint × 4 lift heights (0.03/0.10/0.15/0.20 m)
  all OK. `CUBE_SPAWN_CENTER = (0.32, -0.06, 0.03)` from the brief needed no change — all 4
  corners + center passed on the first probe.
- **Worked as documented ✓ — task-4 brief's Step 3a probe snippet ran verbatim and correctly
  distinguished reachable from unreachable targets** (`solve_ik` + re-verify via a fresh
  `MjData` and `err = ||site_xpos - target||`), once `env/ik.py` existed. Good template for
  Task 5's oracle to reuse directly rather than re-deriving.
- **[none] The task-4 brief's own Step 4 sample code has a bug: `self._render("wrist")` /
  `self._render("scene")` pass literal strings that don't match the actual MJCF camera names**
  (`WRIST_CAM = "wrist_cam"`, not `"wrist"`) — `Renderer.update_scene(camera="wrist")` would
  raise since no such camera exists. Used the `WRIST_CAM`/`SCENE_CAM` config constants instead
  of the brief's literal strings; not caught by any test until actually run (the brief's own
  "expected: 6 passed" would not have been true as literally written). Second brief-code bug
  found in two tasks running (Task 3 found three in `bench_policy.py`) — worth a general
  `testing`/`skill-author` note: spec-provided sample code is not pre-verified and should be
  run, not trusted.
